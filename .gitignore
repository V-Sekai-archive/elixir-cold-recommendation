# Elixir / Mix
_build/
deps/
priv/plts/
.elixir_ls/
cover/
.cover/
erl_crash.dump
*.beam
.mix/
.fetch

# IDE
.idea/
.vscode/
.cursor/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
serve_e2e_data/
bench/output/

# Migration backups (do not commit)
priv/repo/migrations_old/

# RecGPT SQLite (optional catalog/token DB; path from RECGPT_SQLITE_PATH or default)
priv/recgpt.sqlite3
priv/recgpt.sqlite3-shm
priv/recgpt.sqlite3-wal

# Generated DBs (UCI Clickstream, etc.)
data/**/*.db
data/**/*.db-shm
data/**/*.db-wal

# UCI Clickstream (downloaded + extracted + generated)
data/clickstream/*.zip
data/clickstream/*.csv
data/clickstream/e-shop*
data/clickstream/items.json
data/clickstream/train_sequences.json
data/clickstream/test_sequences.json
data/clickstream/cold_test_sequences.json
data/clickstream/cold_train_sequences.json
data/clickstream/fixture.json
data/clickstream/clickstream+data+for+online+shopping/
data/recgpt_layer_3_weight.pt

# FoundationDB (Windows install / build from source)
foundationdb/
fdb_build/
foundationdb-*.msi
foundationdb7-*/
data/recgpt_ckpt_export/*

# EXLA JIT disk cache (compiled XLA binaries)
tmp/exla_cache/

# Nsight Systems GPU profiles (mix recgpt.ad_hoc_test --profile)
*.nsys-rep

# Pipeline / runtime data (Steam, checkpoints, fixture, exports)
data/

# Embedding sanity-check dumps (mix recgpt.compare_embeddings --dump-row N, scripts/embed_one.py)
item*_elixir.raw
item*_sentence_transformers.npy

# Checkpoints (thirdparty/checkpoints) — binaries not committed; use mix recgpt.fetch_vae_ckpt / fetch_ckpt
thirdparty/checkpoints/vae/*.pt
thirdparty/checkpoints/recgpt/*.pt
thirdparty/checkpoints/recgpt/*.npy
thirdparty/checkpoints/recgpt/manifest.json
