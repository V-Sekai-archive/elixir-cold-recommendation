# Elixir / Mix
_build/
deps/
priv/plts/
.elixir_ls/
cover/
.cover/
erl_crash.dump
*.beam
.mix/
.fetch

# Python / uv (scripts/, embedding sanity check)
__pycache__/
*.py[cod]
.venv/
venv/
.uv/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Local env / secrets (never commit)
.env
.env.local
.env.*.local

# IDE
.idea/
.vscode/
.cursor/

# OS
.DS_Store
Thumbs.db

# Logs
*.log
logs/
serve_e2e_data/
bench/output/

# Migration backups (do not commit)
priv/repo/migrations_old/

# RecGPT SQLite (optional catalog/token DB; path from RECGPT_SQLITE_PATH or default)
priv/recgpt.sqlite3
priv/recgpt.sqlite3-shm
priv/recgpt.sqlite3-wal

# Figgie training data and checkpoints
priv/figgie_fixture.json
priv/figgie_checkpoint/
priv/fuxi_checkpoint/

# Ad-hoc / temporary SQLite (e.g. mix recgpt.ad_hoc_test, one-off scripts)
recgpt_adhoc_*.sqlite
*.sqlite3-journal

# Generated DBs
data/**/*.db
data/**/*.db-shm
data/**/*.db-wal

# Pipeline artifacts (refetch via mix recgpt.refetch)

# FoundationDB (Windows install / build from source)
foundationdb/
fdb_build/
foundationdb-*.msi
foundationdb7-*/
data/fuxi_ckpt_export/*

# Torchx (no disk cache by default)

# Nsight Systems GPU profiles (mix recgpt.ad_hoc_test --profile)
*.nsys-rep
*.nsys-rep.sqlite

# Pipeline / runtime data (Steam, checkpoints, fixture, exports)
data/

# Embedding sanity-check dumps (mix recgpt.compare_embeddings --dump-row N, scripts/embed_one.py)
item*_elixir.raw
item*_sentence_transformers.npy

# FuXi-Linear reference (clone for porting; do not commit)
_fuxi_linear_ref/

# Checkpoints (thirdparty/checkpoints) — binaries not committed; use mix recgpt.fetch_vae_ckpt
thirdparty/checkpoints/vae/*.pt
thirdparty/checkpoints/recgpt/*.pt
thirdparty/checkpoints/recgpt/*.npy
thirdparty/checkpoints/recgpt/manifest.json

# KuaiRand-Pure (bulk CSVs; fetch from Zenodo / kuairand.com)
thirdparty/KuaiRand-Pure/*.csv

# Temp / scratch
tmp/
