# Test fixtures

## sample.pt

Minimal PyTorch `.pt` checkpoint (zip or legacy pickle) for manual testing of `RecGPT.PtLoader`. Not required for the default test run; PtLoader tests cover invalid-file and file-not-found behavior.
